import { InfoLinkDirective } from './info-link.directive';

describe('InfoLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoLinkDirective();
    expect(directive).toBeTruthy();
  });
});
